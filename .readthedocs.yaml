version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    pre_build:
      - bash automation/fetch_remote.py
      - python3 automation/generate_TOC.py

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: requirements.txt
